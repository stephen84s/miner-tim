[
  {
    "diff": "--- src/pool_connection.rs\n+++ replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![])\n@@ -869,28 +869,17 @@\n /// stream kills the process (GitHub #21).\n ///\n /// Splitting messages across reads is normal and must keep working, which is why\n /// the check is on the *remainder after draining* rather than on the buffer as it\n /// arrives. Returning whole lines only is also what keeps a truncated message\n /// from ever reaching `handle_pool_message`, so an oversized message cannot leave\n /// a partial job active.\n fn take_complete_lines(pending: &mut Vec<u8>) -> Result<Vec<String>, usize> {\n-    let mut lines = Vec::new();\n-    while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n-        let raw: Vec<u8> = pending.drain(..=pos).collect();\n-        let line = String::from_utf8_lossy(&raw).trim().to_string();\n-        if !line.is_empty() {\n-            lines.push(line);\n-        }\n-    }\n-    if pending.len() > MAX_LINE_BYTES {\n-        return Err(pending.len());\n-    }\n-    Ok(lines)\n+    Ok(vec![]) /* ~ changed by cargo-mutants ~ */\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n     let job_id = data.get(\"job_id\")?.as_str()?.to_string();\n     let seed_hash_hex = data.get(\"seed_hash\")?.as_str()?;\n \n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "FnValue",
    "name": "src/pool_connection.rs:877:5: replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![])",
    "package": "minertim",
    "replacement": "Ok(vec![])",
    "span": {
      "end": {
        "column": 14,
        "line": 888
      },
      "start": {
        "column": 5,
        "line": 877
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![String::new()])\n@@ -869,28 +869,17 @@\n /// stream kills the process (GitHub #21).\n ///\n /// Splitting messages across reads is normal and must keep working, which is why\n /// the check is on the *remainder after draining* rather than on the buffer as it\n /// arrives. Returning whole lines only is also what keeps a truncated message\n /// from ever reaching `handle_pool_message`, so an oversized message cannot leave\n /// a partial job active.\n fn take_complete_lines(pending: &mut Vec<u8>) -> Result<Vec<String>, usize> {\n-    let mut lines = Vec::new();\n-    while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n-        let raw: Vec<u8> = pending.drain(..=pos).collect();\n-        let line = String::from_utf8_lossy(&raw).trim().to_string();\n-        if !line.is_empty() {\n-            lines.push(line);\n-        }\n-    }\n-    if pending.len() > MAX_LINE_BYTES {\n-        return Err(pending.len());\n-    }\n-    Ok(lines)\n+    Ok(vec![String::new()]) /* ~ changed by cargo-mutants ~ */\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n     let job_id = data.get(\"job_id\")?.as_str()?.to_string();\n     let seed_hash_hex = data.get(\"seed_hash\")?.as_str()?;\n \n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "FnValue",
    "name": "src/pool_connection.rs:877:5: replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![String::new()])",
    "package": "minertim",
    "replacement": "Ok(vec![String::new()])",
    "span": {
      "end": {
        "column": 14,
        "line": 888
      },
      "start": {
        "column": 5,
        "line": 877
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![\"xyzzy\".into()])\n@@ -869,28 +869,17 @@\n /// stream kills the process (GitHub #21).\n ///\n /// Splitting messages across reads is normal and must keep working, which is why\n /// the check is on the *remainder after draining* rather than on the buffer as it\n /// arrives. Returning whole lines only is also what keeps a truncated message\n /// from ever reaching `handle_pool_message`, so an oversized message cannot leave\n /// a partial job active.\n fn take_complete_lines(pending: &mut Vec<u8>) -> Result<Vec<String>, usize> {\n-    let mut lines = Vec::new();\n-    while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n-        let raw: Vec<u8> = pending.drain(..=pos).collect();\n-        let line = String::from_utf8_lossy(&raw).trim().to_string();\n-        if !line.is_empty() {\n-            lines.push(line);\n-        }\n-    }\n-    if pending.len() > MAX_LINE_BYTES {\n-        return Err(pending.len());\n-    }\n-    Ok(lines)\n+    Ok(vec![\"xyzzy\".into()]) /* ~ changed by cargo-mutants ~ */\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n     let job_id = data.get(\"job_id\")?.as_str()?.to_string();\n     let seed_hash_hex = data.get(\"seed_hash\")?.as_str()?;\n \n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "FnValue",
    "name": "src/pool_connection.rs:877:5: replace take_complete_lines -> Result<Vec<String>, usize> with Ok(vec![\"xyzzy\".into()])",
    "package": "minertim",
    "replacement": "Ok(vec![\"xyzzy\".into()])",
    "span": {
      "end": {
        "column": 14,
        "line": 888
      },
      "start": {
        "column": 5,
        "line": 877
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace == with != in take_complete_lines\n@@ -870,17 +870,17 @@\n ///\n /// Splitting messages across reads is normal and must keep working, which is why\n /// the check is on the *remainder after draining* rather than on the buffer as it\n /// arrives. Returning whole lines only is also what keeps a truncated message\n /// from ever reaching `handle_pool_message`, so an oversized message cannot leave\n /// a partial job active.\n fn take_complete_lines(pending: &mut Vec<u8>) -> Result<Vec<String>, usize> {\n     let mut lines = Vec::new();\n-    while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n+    while let Some(pos) = pending.iter().position(|&b| b != /* ~ changed by cargo-mutants ~ */ b'\\n') {\n         let raw: Vec<u8> = pending.drain(..=pos).collect();\n         let line = String::from_utf8_lossy(&raw).trim().to_string();\n         if !line.is_empty() {\n             lines.push(line);\n         }\n     }\n     if pending.len() > MAX_LINE_BYTES {\n         return Err(pending.len());\n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "BinaryOperator",
    "name": "src/pool_connection.rs:878:58: replace == with != in take_complete_lines",
    "package": "minertim",
    "replacement": "!=",
    "span": {
      "end": {
        "column": 60,
        "line": 878
      },
      "start": {
        "column": 58,
        "line": 878
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ delete ! in take_complete_lines\n@@ -873,17 +873,17 @@\n /// arrives. Returning whole lines only is also what keeps a truncated message\n /// from ever reaching `handle_pool_message`, so an oversized message cannot leave\n /// a partial job active.\n fn take_complete_lines(pending: &mut Vec<u8>) -> Result<Vec<String>, usize> {\n     let mut lines = Vec::new();\n     while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n         let raw: Vec<u8> = pending.drain(..=pos).collect();\n         let line = String::from_utf8_lossy(&raw).trim().to_string();\n-        if !line.is_empty() {\n+        if  /* ~ changed by cargo-mutants ~ */line.is_empty() {\n             lines.push(line);\n         }\n     }\n     if pending.len() > MAX_LINE_BYTES {\n         return Err(pending.len());\n     }\n     Ok(lines)\n }\n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "UnaryOperator",
    "name": "src/pool_connection.rs:881:12: delete ! in take_complete_lines",
    "package": "minertim",
    "replacement": "",
    "span": {
      "end": {
        "column": 13,
        "line": 881
      },
      "start": {
        "column": 12,
        "line": 881
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace > with == in take_complete_lines\n@@ -877,17 +877,17 @@\n     let mut lines = Vec::new();\n     while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n         let raw: Vec<u8> = pending.drain(..=pos).collect();\n         let line = String::from_utf8_lossy(&raw).trim().to_string();\n         if !line.is_empty() {\n             lines.push(line);\n         }\n     }\n-    if pending.len() > MAX_LINE_BYTES {\n+    if pending.len() == /* ~ changed by cargo-mutants ~ */ MAX_LINE_BYTES {\n         return Err(pending.len());\n     }\n     Ok(lines)\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "BinaryOperator",
    "name": "src/pool_connection.rs:885:22: replace > with == in take_complete_lines",
    "package": "minertim",
    "replacement": "==",
    "span": {
      "end": {
        "column": 23,
        "line": 885
      },
      "start": {
        "column": 22,
        "line": 885
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace > with < in take_complete_lines\n@@ -877,17 +877,17 @@\n     let mut lines = Vec::new();\n     while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n         let raw: Vec<u8> = pending.drain(..=pos).collect();\n         let line = String::from_utf8_lossy(&raw).trim().to_string();\n         if !line.is_empty() {\n             lines.push(line);\n         }\n     }\n-    if pending.len() > MAX_LINE_BYTES {\n+    if pending.len() < /* ~ changed by cargo-mutants ~ */ MAX_LINE_BYTES {\n         return Err(pending.len());\n     }\n     Ok(lines)\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "BinaryOperator",
    "name": "src/pool_connection.rs:885:22: replace > with < in take_complete_lines",
    "package": "minertim",
    "replacement": "<",
    "span": {
      "end": {
        "column": 23,
        "line": 885
      },
      "start": {
        "column": 22,
        "line": 885
      }
    }
  },
  {
    "diff": "--- src/pool_connection.rs\n+++ replace > with >= in take_complete_lines\n@@ -877,17 +877,17 @@\n     let mut lines = Vec::new();\n     while let Some(pos) = pending.iter().position(|&b| b == b'\\n') {\n         let raw: Vec<u8> = pending.drain(..=pos).collect();\n         let line = String::from_utf8_lossy(&raw).trim().to_string();\n         if !line.is_empty() {\n             lines.push(line);\n         }\n     }\n-    if pending.len() > MAX_LINE_BYTES {\n+    if pending.len() >= /* ~ changed by cargo-mutants ~ */ MAX_LINE_BYTES {\n         return Err(pending.len());\n     }\n     Ok(lines)\n }\n \n fn parse_job(data: &Value) -> Option<Job> {\n     let blob_hex = data.get(\"blob\")?.as_str()?;\n     let target_hex = data.get(\"target\")?.as_str()?;\n",
    "file": "src/pool_connection.rs",
    "function": {
      "function_name": "take_complete_lines",
      "return_type": "-> Result<Vec<String>, usize>",
      "span": {
        "end": {
          "column": 2,
          "line": 889
        },
        "start": {
          "column": 1,
          "line": 863
        }
      }
    },
    "genre": "BinaryOperator",
    "name": "src/pool_connection.rs:885:22: replace > with >= in take_complete_lines",
    "package": "minertim",
    "replacement": ">=",
    "span": {
      "end": {
        "column": 23,
        "line": 885
      },
      "start": {
        "column": 22,
        "line": 885
      }
    }
  }
]