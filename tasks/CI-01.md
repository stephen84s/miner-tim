# CI-01 — CI rewrite.

**Status:** Completed

Replaced stale Android/Gradle pipeline with lint (clippy -D warnings), audit, and test jobs for the root CLI crate.

---

*Full record: the `CI-01` entry in [`AUDIT.md`](../AUDIT.md), which is authoritative. This file is the summary.*
