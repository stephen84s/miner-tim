# NET-01 — Pool robustness.

**Status:** Completed

Fixed TLS double-session receiver bug; added keepalive, auto-reconnect/relogin, 8-byte target support. Clippy backlog cleared (63→0).

---

*Full record: the `NET-01` entry in [`AUDIT.md`](../AUDIT.md), which is authoritative. This file is the summary.*
